<div class="row">
	<div class="col-5 mx-auto">
		{{ form_start(form) }}
		{# {{ form_label(form.title, 'Nom de l\'annonce', {'label_attr': {'class': 'foo'}}) }} #}

		{{ form_widget(form) }}

		<button class="btn btn-primary">{{ button_label|default('Sauvegarder') }}</button>
		{{ form_end(form) }}
	</div>
</div>
