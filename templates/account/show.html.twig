{% extends 'base.html.twig' %}

{% block title %}
	Mes Informations | DealGames
{% endblock %}

{% block body %}
	<div class="row">
		<div class="col-12 col-md-9 col-lg-6 mx-auto p-5 shadow-custom rounded-3">
			<h1 class="mb-4">Mes Informations</h1>
			<div class="mb-3">
				<label class="form-label">Adresse mail</label>
				<input type="email" value="{{ app.user.email }}" class="form-control" disabled>
			</div>
			<div class="mb-3">
				<label class="form-label">Prénom</label>
				<input type="email" value="{{ app.user.firstname }}" class="form-control" disabled>
			</div>
			<div class="mb-3">
				<label class="form-label">Nom</label>
				<input type="email" value="{{ app.user.lastname }}" class="form-control" disabled>
			</div>
			<span class="d-flex justify-content-center mt-4">
				<a href="{{ path('app_account_profil_edit') }}" class="btn btn-primary btn-lg">Modifier mes Informations</a>
			</span>
		</div>
	</div>

{% endblock %}
